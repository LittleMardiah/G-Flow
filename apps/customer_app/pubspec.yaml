name: customer_app
description: G-Flow Customer App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.13.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.3.0
  riverpod: ^2.4.0
  flutter_riverpod: ^2.4.0
  flutter_map: ^6.1.0          # gratis, tanpa API key
  latlong2: ^0.9.0            # untuk koordinat
  geolocator: ^10.0.0
  flutter_secure_storage: ^9.0.0
  uuid: ^4.3.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
