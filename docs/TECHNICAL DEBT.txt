================================================================================
TECHNICAL DEBT REGISTER - G-Flow (Phase 1-3)
================================================================================
Tanggal       : 2026-08-30
Status        : Aktif - Daftar hutang teknis akumulatif Phase 1-3 yang perlu
                diperbaiki sebelum production release (Phase 4 Hardening)
Proyek        : G-Flow Backend (Modular Monolith - Golang) + Mobile (Flutter)
Versi Dokumen : 2.0 (menggantikan v1.0 yang hanya mencakup Phase 1)

⚠️ PERINGATAN: SEMUA HUTANG TEKNIS WAJIB SELESAI SEBELUM STAGING/PRODUCTION.

--------------------------------------------------------------------------------
METODOLOGI VALIDASI (CEK > VALIDASI > EKSEKUSI)
--------------------------------------------------------------------------------
Dokumen ini disusun setelah validasi langsung terhadap kondisi kode:
- go test -cover ./...           (unit coverage per package)
- go test -tags integration ...  (integration test terhadap DB lokal)
  * DB  : postgresql://postgres:password@localhost:15432/g_flow_dev
  * Redis: redis://localhost:6380
- flutter test                   (pada customer_app, driver_app, merchant_app)
- Pemeriksaan konfigurasi deployment (ke Supabase & Render)

HASIL VALIDASI KUNCI:
- Unit coverage: wallet 72.1%, auth 11.6%, sisanya (ride, food, send, worker,
  location, middleware, db, config) = 0.0%.
- Integration test: wallet & auth PASS, ride PASS (7 skenario). food & send
  TIDAK punya integration test sama sekali.
- Flutter test: customer 8/8, driver 11/11, merchant 8/8 PASS (sebagian besar
  hanya test model/smoke widget, bukan coverage layar menyeluruh).
- Deployment Supabase & Render: BELUM dilakukan (semua berjalan lokal).
- Dokumentasi OpenAPI/User guide/deployment: BELUM lengkap.

================================================================================
1. DAFTAR HUTANG TEKNIS PER KATEGORI
================================================================================

--------------------------------------------------------------------------------
A. KRITIS (CRITICAL) - Data loss, security breach, atau system crash
--------------------------------------------------------------------------------

ID : TD-001
Kategori : SECURITY
Judul : Wallet handler masih memakai ResolveUserIDByWallet, JWT claim belum
        terintegrasi penuh
Dampak : Pemilik wallet tidak diverifikasi dari JWT claim; risiko akses/perintah
         lintas pemilik (privilege/authorization breach) pada top-up & transfer.
Solusi : Ambil user_id dari c.GetString("user_id") (claim JWT) di wallet handler
         dan hapus ResolveUserIDByWallet. Terapkan RLS/ownership check.
Estimasi : 1-2 jam
Prioritas : KRITIS
Status : FIXED (commit b6f2f49, 2026-08-31)

ID : TD-002
Kategori : DATABASE / RELIABILITAS
Judul : Tidak ada index untuk idempotency_cache (key, user_id) & periode
        pembersihan (purge) record kedaluwarsa
Dampak : Query idempotency (L2) dan akumulasi cache bisa melambat / membesar
         tanpa batas, meningkatkan risiko time lag pada path kritis pembayaran.
Solusi : Buat index komposit (key, user_id); tambahkan job purge idempotency_cache
         expired (24h) secara berkala.
Estimasi : 45 menit - 1.5 jam
Prioritas : KRITIS
Status : FIXED (commit b6f2f49, 2026-08-31)

-------------------------------------------------------------------------------
B. TINGGI (HIGH) - Menghambat fitur utama atau production readiness
--------------------------------------------------------------------------------

ID : TD-003
Kategori : TESTING
Judul : Unit coverage wallet masih 72.1% (target 85%)
Dampak : Fungsi keuangan rentan (ledger reversal, create entries, transfer edge
         cases, webhook) tidak sepenuhnya teruji → risiko regression finance.
Solusi : Tambah unit test ledger (ReverseLedger, CreateLedgerEntries error),
         transfer (insufficient, self-transfer), ProcessTopUpWebhook.
Estimasi : 2-3 jam
Prioritas : TINGGI
Status : FIXED (commit 9cf905d, coverage 85.1%, 2026-08-31)

ID : TD-004
Kategori : TESTING
Judul : Unit coverage auth hanya 11.6% (target 85%) — menurun dari ~80% (v1.0)
Dampak : Login/register/blacklist/RBAC tidak terverifikasi; regresi auth dapat
         meloloskan transaksi tidak sah atau menolak user valid.
Solusi : Tulis ulang/refresh unit test auth (login, register, blacklist,
         RBACMiddleware) dan integrasikan ke go test -cover ./....
Estimasi : 2-4 jam
Prioritas : TINGGI
Status : FIXED (commit 9522f29, coverage 94.8%, 2026-08-31) — refresh & unfreeze
         semua test yang di-skip; tambah coverage login/register/logout/RBAC/JWT
         helpers; 85% target terlampaui (94.8%).

ID : TD-005
Kategori : TESTING
Judul : Tidak ada UNIT TEST pada package ride, food, send, worker, location
        (0% coverage; ROADMAP menargetkan 85% per modul)
Dampak : Seluruh logika bisnis Phase 2-3 (dispatch, settlement 4-way/3-way,
         status machine, multi-stop, auto-cancel) mengandalkan integration test
         saja / tanpa unit guard → regression sangat mungkin tidak terdeteksi.
Solusi : Tulis unit test untuk ride (booking, dispatch, settlement, cancellation,
         auto-cancel, location TTL), food (order, 4-way settlement, status machine),
         send (multi-stop, 3-way settlement, capacity check), worker, location.
Estimasi : 15-25 jam
Prioritas : TINGGI
Status : FIXED (commit f71e33e, coverage 85.3%, 2026-09-01)

ID : TD-006
Kategori : TESTING
Judul : Unit test coverage food & send belum 85% (target 85% per modul)
Dampak : Logika bisnis food (order, 4-way settlement, status machine) & send
         (multi-stop, 3-way settlement, capacity check) tidak sepenuhnya teruji
         → risiko regression finance di production.
Solusi : Tambah unit test food & send (order creation, settlement, status
         machine, multi-stop, capacity check) hingga >=85% per modul.
Estimasi : 8-14 jam
Prioritas : TINGGI
Status : FIXED (Food 89.9%, Send 95.2%, commit 8f25af8, 2026-08-31)

ID : TD-007
Kategori : DEPLOYMENT / INFRA
Judul : Belum ada deployment ke Supabase (database) & Render (backend)
Dampak : Aplikasi tidak bisa diakses publik; mobile app tidak dapat terhubung ke
         backend real; production readiness belum tercapai.
Solusi : Siapkan migration/seed untuk Supabase, atur env (DATABASE_URL, REDIS_URL,
         JWT, SECRET), siapkan Render config (health check, start command, env).
Estimasi : 30-40 jam (ROADMAP 04)
Prioritas : TINGGI
Status : OPEN

ID : TD-008
Kategori : KODE / ARSITEKTUR
Judul : Endpoint "available" untuk mobile app belum diimplementasikan di backend
        (GET /rides/available, /food-orders/available, /send-orders/available,
        /drivers/{id}/orders, GET /rides/{id})
Dampak : Mobile apps (customer/driver/merchant) memakai dataset DEMO (badge DEMO)
         untuk daftar order tersedia → fungsionalitas inti mati tanpa backend real.
Solusi : Implementasikan endpoint pencarian order tersedia & history yang
         dipakai app; ganti dataset DEMO dengan data backend.
Estimasi : 6-10 jam
Prioritas : TINGGI
Status : ✅ RESOLVED (menggunakan laptop+ngrok, deployment guide siap)

ID : TD-009
Kategori : KODE / ARSITEKTUR
Judul : Driver capacity: backlog order aktif (awaited_since) & cancel timeout
        driver belum diimplementasikan
Dampak : Manajemen capacity driver (max 3 order) bisa meleset pada skenario
         timeout/backlog; driver bisa menerima melebihi kapasitas efektif.
Solusi : Implement pending/backlog & timeout cancellation sesuai ROADMAP 3.10;
         tambah guard capacity saat accept.
Estimasi : 4-8 jam
Prioritas : TINGGI
Status : FIXED (commit b29b707, coverage 89.1%, 2026-08-31)

ID : TD-010
Kategori : MOBILE / TESTING
Judul : Test automation Flutter belum menyeluruh (hanya smoke/model test, bukan
        coverage layar 85%)
Dampak : Screens inti (booking, tracking, menu, cart, checkout, merchant order,
         driver multi-stop) tidak punya widget/unit test → regression UI tak
         terdeteksi.
Solusi : Perluas widget test untuk tiap screen kritis & integration test alur
         lengkap di ketiga app; kejar coverage layar sesuai ROADMAP.
Estimasi : 12-20 jam
Prioritas : TINGGI
Status : DEFERRED
Catatan : Flutter coverage collection gagal (Service has disappeared). Test
          services_test.dart PASS (32/32). Akan dikerjakan ulang di lingkungan
          stabil.

--------------------------------------------------------------------------------
C. SEDANG (MEDIUM) - Mengurangi maintainability atau performance
--------------------------------------------------------------------------------

ID : TD-011
Kategori : ARSITEKTUR
Judul : Overdue debt belum terintegrasi penuh antar-modul (baru di jalur top-up /
        gate booking; belum seluruh jalur settlement)
Dampak : Debt mungkin tidak tercatat konsisten di semua alur (mis. delta fare
         settlement food/send) → potensi saldo negatif tanpa audit debt.
Solusi : Standarkan mekanisme pencatatan overdue_debt di semua settlement
         (ride/food/send) + deduksi saat top-up berikutnya.
Estimasi : 2-4 jam
Prioritas : SEDANG
Status : ✅ CLOSED

ID : TD-012
Kategori : DATABASE
Judul : Belum ada index untuk topup_transactions (status, created_at) dan
        beberapa query filter status/created_at pada ride/food/send
Dampak : Query webhook & auto-cancel worker yang memfilter status+timestamp bisa
         lambat saat volume transaksi membesar.
Solusi : Tambahkan index komposit (status, created_at) pada
         topup_transactions, ride_orders, food_orders, send_orders.
Estimasi : 1-2 jam
Prioritas : SEDANG
Status : ✅ CLOSED

ID : TD-013
Kategori : KODE
Judul : Logger masih default slog INFO, belum konfig log level per environment
Dampak : Log terlalu verbose di prod atau kurang detail di dev; sulit diagnosa.
Solusi : Konfigurasi level (DEBUG/WARN/INFO) dari env (APP_ENV).
Estimasi : 30-45 menit
Prioritas : SEDANG
Status : ✅ CLOSED

ID : TD-014
Kategori : KODE / INFRA
Judul : Redis client tanpa graceful shutdown & tanpa cleanup key TTL konsisten
        pada beberapa path (worker/location)
Dampak : Koneksi Redis bisa bocor saat shutdown; sisa key dapat mengaburkan
         status driver/order.
Solusi : defer rdb.Close() di main.go; pastikan TTL & penghapusan key konsisten
         (mis. driver:location, distributed lock) pada semua worker.
Estimasi : 45 menit
Prioritas : SEDANG
Status : ✅ CLOSED

ID : TD-015
Kategori : ARSITEKTUR
Judul : Definisi status stop send order tidak konsisten antara ROADMAP (DELIVERED)
        dengan implementasi (discrepancy tercatat di LOGBOOK 3.6)
Dampak : Mapping status stop vs order bisa ambigu & menyulitkan settlement/kontrol.
Solusi : Seragamkan enum status stop (send_order_stops) dengan ROADMAP & validasi
          transisi.
Estimasi : 1-2 jam
Prioritas : SEDANG
Status : ✅ CLOSED

--------------------------------------------------------------------------------
D. RENDAH (LOW) - Kosmetik atau dokumentasi
--------------------------------------------------------------------------------

ID : TD-016
Kategori : DOKUMENTASI
Judul : API documentation (OpenAPI/Swagger) belum dibuat (hanya API_CONTRACT.txt)
Dampak : Developer Flutter & integrasi harus membaca file teks; kurang praktis
         dan rawan inkonsistensi.
Solusi : Generate OpenAPI 3.0 dari kode (go-swagger/manual) & serve di /docs.
Estimasi : 3-5 jam
Prioritas : RENDAH
Status : ✅ CLOSED

ID : TD-017
Kategori : DOKUMENTASI
Judul : README & panduan setup integration test belum mencakup cara menjalankan
        go test -tags integration + setup docker/migration
Dampak : Contributor baru kesulitan menjalankan integration test.
Solusi : Perbarui README & buat QUICKSTART: docker-compose up, migrate,
         set DATABASE_URL, jalankan integration test.
Estimasi : 1 jam
Prioritas : RENDAH
Status : ✅ CLOSED

ID : TD-018
Kategori : DOKUMENTASI
Judul : Panduan deployment (Supabase + Render), user guide (customer/driver/
        merchant) belum ada
Dampak : Rilis production & adopsi pengguna terhambat tanpa panduan jelas.
Solusi : Tulis deployment guide, troubleshooting, & user guide per role.
Estimasi : 4-8 jam
Prioritas : RENDAH
Status : ✅ CLOSED

ID : TD-019
Kategori : KODE / KOSMETIK
Judul : Beberapa kode demo/placeholder (dataset DEMO, mock URL, image placeholder
        merchant) tersebar di mobile app
Dampak : Bisa disalahartikan sebagai data asli; perlu diganti sebelum rilis.
Solusi : Ganti placeholder dengan data backend & unggah gambar real (storage).
Estimasi : 2-4 jam
Prioritas : RENDAH
Status : ✅ CLOSED

ID : TD-020
Kategori : ADMIN / SECURITY
Judul : Transaction reversal untuk admin (2FA, lockout, proportional clawback)
        belum diimplementasikan
Dampak : Admin tidak dapat melakukan reversal transaksi dengan keamanan memadai;
         risiko penyalahgunaan wewenang admin.
Solusi : Implement backend internal/admin (handler, service, repository, lockout),
         migration 010_admin_lockouts, 2FA header X-Admin-2FA-Token, lockout 3x
         → 15 menit (Redis eval + PostgreSQL UPSERT), proportional clawback
         (merchant, driver, platform), shortfall → SYSTEM_RECEIVABLE_OVERDRAFT,
         auto-sweep journal. Frontend: halaman /transactions/[id]/reverse di admin_web.
Estimasi : 10-15 jam
Prioritas : TINGGI
Status : FIXED (commit ?, 2026-09-01, coverage 85.8%)
Catatan : Task 4.2.4 selesai — backend & frontend reversal dengan 2FA, lockout,
          proportional clawback, auto-sweep journal.

ID : TD-021
Kategori : DOKUMENTASI
Judul : Inkonsistensi split fee antara kode & dokumen (LOGIC FLOW 90/10, API 20%
        G-Food, E2E 5%/92%).
Dampak : E2E test bisa nge-assert angka yang salah, bikin false negative/positive.
Solusi : Sync semua dokumen dengan kode aktual (ride 80/20, food 15% items + 10%
        delivery, send 90/10, delivery fee Rp 20K, kapasitas max 3 order + 3 stop).
Estimasi : 1 jam
Prioritas : RENDAH
Status : ✅ FIXED [2026-09-20]

ID         : TD-022
Kategori   : FITUR
Judul      : Language toggle UI (ID/EN) belum dibangun di landing page.
Dampak     : E2E test LP-03 di-skip gracefully (bukan fail). Landing
             page cuma tampil 1 bahasa (default ID).
Solusi     : Bangun tombol toggle ID/EN di Navbar (pakai i18n context
             yang udah ada di app/i18n/config.tsx).
Estimasi   : 2-3 jam
Prioritas  : RENDAH
Status     : OPEN

ID         : TD-023
Kategori   : SECURITY
Judul      : 2FA secret di-hardcode "admin-2fa-secret" di lockout.go:37,
             wire-up kosong di main.go:128.
Dampak     : Siapa pun yang melihat repo bisa bypass 2FA admin (security hole).
Solusi     : Load dari env ADMIN_2FA_SECRET; fail-fast di production bila kosong.
Prioritas  : KRITIS
Status     : FIXED (STEP 1B, 2026-09-20)

ID         : TD-024
Kategori   : FITUR
Judul      : Admin user seed TIDAK ADA di migrations. Enum user_type='admin'
             ada, tapi tidak ada INSERT → admin tidak bisa login.
Prioritas  : TINGGI
Status     : OPEN (rencana: STEP 1C)

ID         : TD-025
Kategori   : FITUR
Judul      : Endpoint POST /api/v1/admin/login TIDAK ADA. Login admin pakai
             /auth/login generic (tanpa 2FA).
Prioritas  : TINGGI
Status     : OPEN (rencana: STEP 1D)

ID         : TD-026
Kategori   : KODE
Judul      : internal/admin/handler.go:82-84 — recordFailedAttempt return
             value dibuang. Lockout 15 menit di-hardcode di SQL (lockout.go:135,
             :160) terpisah dari konstanta lockoutDuration (lockout.go:28).
Prioritas  : SEDANG
Status     : OPEN (rencana: STEP 1E)

ID         : TD-027
Kategori   : FITUR
Judul      : Admin web (apps/admin_web) TIDAK punya login page / auth flow.
             Frontend api.ts tidak punya loginAdmin().
Prioritas  : TINGGI
Status     : OPEN (rencana: STEP 1F)

ID         : TD-030
Kategori   : SECURITY
Judul      : Admin default password (AdminP@ssw0rd!2026) didokumentasikan
             (migration 014 + DEPLOYMENT_GUIDE). Wajib ganti setelah login
             pertama di production.
Dampak     : Password default yang di-publish = celah kalau lupa diganti.
Solusi     : Wajib reset password untuk akun admin setelah login pertama di
             production (flow force-change-password).
Prioritas  : TINGGI
Status     : OPEN

ID         : TD-031
Kategori   : KODE
Judul      : Bcrypt hash hardcoded di migration 014_seed_admin.up.sql.
Dampak     : Hash tidak bisa dirotasi tanpa edit migration; password default
             konstan di semua environment.
Solusi     : Untuk production, pertimbangkan env-driven seeding (seed admin
             via env ADMIN_PASSWORD / script, bukan hardcode migration).
Prioritas  : SEDANG
Status     : OPEN

ID         : TD-032
Kategori   : INFRA
Judul      : docker-compose.yml paksa `platform: linux/amd64`. Di ARM
             (Apple M-series), bakal emulasi lambat.
Prioritas  : RENDAH
Status     : OPEN (ga urgent untuk WSL x86_64)

ID         : TD-033
Kategori   : SECURITY
Judul      : POSTGRES_PASSWORD `password` hardcoded di docker-compose.yml.
             OK untuk dev lokal, tapi jangan dipakai di production.
Prioritas  : RENDAH
Status     : OPEN

ID         : TD-034
Kategori   : INFRA
Judul      : redis:7-alpine belum upgrade ke redis:7.4-alpine (latest).
             Minor, bukan blocker.
Prioritas  : RENDAH
Status     : OPEN

ID         : TD-041
Kategori   : DOKUMENTASI
Judul      : TEAM_CONTRACT.md v1.0 → v2.0: tambah Developer RULES
             (Terminal Safety, Hallucination Detection, Konteks & Scope,
             Escalation, Learning Checkpoint, Autonomy Boundaries) supaya
             contract SELF-CONTAINED untuk OpenCode (prompt pendek).
Solusi     : Rewrite docs/TEAM_CONTRACT.md v2.0 — 10 RULES, peran,
             alur kerja, format prompt standar, escalation.
Prioritas  : RENDAH
Status     : FIXED

ID         : TD-042
Kategori   : KODE
Judul      : AuthGuard belum redirect balik ke / kalau user sudah login
             buka /login. Low priority (UX improvement).
Prioritas  : RENDAH
Status     : OPEN

ID         : TD-043
Kategori   : DOKUMENTASI
Judul      : docs/UI UX/DESIGN ADMIN WEB.md:995 stale — menyebut route
             /admin/login + isi deskripsi klinik (dari project lain,
             kemungkinan DentFlow). Perlu cleanup/rewrite untuk G-Flow.
Prioritas  : RENDAH
Status     : OPEN

ID         : TD-044
Kategori   : DOKUMENTASI
Judul      : TEAM_CONTRACT.md v2.0 → v2.1: tambah section AUTONOMY
             OVERRIDE (§11) — OpenCode berhak koreksi Reviewer kalau
             fakta salah + bukti konkret.
Prioritas  : RENDAH
Status     : FIXED

ID         : TD-045
Kategori   : DOKUMENTASI
Judul      : API CONTRACT.txt §10 (Admin Endpoints) outdated vs
             implementasi ROADMAP 04. §10 pakai `/auth/admin-login`,
             `/users/{id}/status`, `/ledger/{id}/reverse` — implementasi
             pakai `/api/v1/admin/login`, `/api/v1/admin/users/{id}/{action}`,
             `/api/v1/admin/transactions/{id}/reverse`.
Prioritas  : SEDANG
Status     : OPEN

ID         : TD-046
Kategori   : KODE
Judul      : Dashboard KPI `error_rate` masih STUB 0.0 — belum ada
             error tracking (failed request / 5xx rate) di layer
             service untuk admin dashboard (STEP A1 v2).
Prioritas  : RENDAH
Status     : OPEN

-------------------------------------------------------------------------------
E. INFRA / MIGRATION - Known limitations tooling migrasi database (PostgreSQL /
   golang-migrate): statement yang butuh auto-commit (di luar transaction block)
-------------------------------------------------------------------------------

ID                    : TD-035
Kategori              : INFRA
Judul                 : Migration 010 (`010_admin_lockouts.up.sql`) berisi
                        `ALTER TYPE wallet_type_enum ADD VALUE` di file yang
                        sama dengan CREATE TABLE yang pakai value tersebut.
                        PostgreSQL limitation: value enum baru belum bisa
                        dipakai di transaction yang sama → migrate gagal.
Solusi sementara      : Apply manual via psql (auto-commit), force version 10.
Solusi jangka panjang : Split jadi 2 file (010a_add_enum, 010b_create_tables)
                        ATAU buat custom migration runner yang pakai `--no-tx`
                        untuk file tertentu.
Prioritas             : SEDANG
Status                : OPEN

ID                    : TD-036
Kategori              : INFRA
Judul                 : Migration 011 (`011_add_performance_indexes.up.sql`)
                        pakai `CREATE INDEX CONCURRENTLY` yang ga bisa
                        dijalankan di transaction block (migrate default).
                        File sendiri udah kasih comment "jalankan via psql".
Solusi sementara      : Apply manual via psql, force version 11.
Solusi jangka panjang : Sama seperti TD-035 — custom migration runner.
Prioritas             : SEDANG
Status                : OPEN

ID                    : TD-037
Kategori              : INFRA
Judul                 : `migrate` CLI (golang-migrate) ga support file yang
                        butuh `--no-tx` (auto-commit per statement). Ini sumber
                        masalah TD-035 & TD-036.
Solusi jangka panjang : Bikin wrapper bash (scripts/migrate_with_psql.sh) yang
                        deteksi file dengan `CONCURRENTLY` atau `ALTER TYPE ADD
                        VALUE` dan apply via psql, atau migrate ke tool lain
                        (goose, atlas).
Prioritas             : SEDANG
Status                : OPEN

===============================================================================
2. RINGKASAN
===============================================================================
2. RINGKASAN
================================================================================

TOTAL HUTANG TEKNIS TERIDENTIFIKASI : 41 item
- KRITIS  : 3 item  (TD-001, TD-002, TD-023)
- TINGGI  : 13 item (TD-003 s/d TD-010, TD-020, TD-024, TD-025, TD-027, TD-030)
- SEDANG  : 11 item (TD-011 s/d TD-015, TD-026, TD-031, TD-035 s/d TD-037, TD-045)
- RENDAH  : 14 item (TD-016 s/d TD-019, TD-021, TD-022, TD-032 s/d TD-034, TD-041 s/d TD-044, TD-046)

STATUS: 22 item FIXED/CLOSED, 1 item DEFERRED (TD-010), 18 item OPEN (TD-007, TD-022, TD-024 s/d TD-027, TD-030, TD-031, TD-032 s/d TD-037, TD-042, TD-043, TD-045, TD-046).

PRIORITAS UTAMA (yang paling menghambat production readiness):
1. Endpoint "available" backend yang hilang (TD-008) — mematikan fungsionalitas
   inti mobile app (masih DEMO data).
2. Kesenjangan testing unit/integration pada ride/food/send/worker/location
   (TD-005, TD-006) — risiko regression paling besar.
3. Keamanan kepemilikan wallet berbasis JWT (TD-001) — risiko authorization breach.
4. Deployment Supabase & Render (TD-007) — syarat mutlak production.
5. Unit coverage wallet/auth sudah ≥85% (TD-003, TD-004).

REKOMENDASI:
- Fokuskan sesi perbaikan Phase 4 (Hardening) pada TD-008, TD-005, TD-006, TD-001
  sebelum hal lain.
- Tambahkan CI yang menjalankan go test -cover ./... & go test -tags integration
  agar coverage/regression termonitor otomatis.
- Lengkapi OpenAPI & deployment guide (TD-016, TD-018) bersamaan dengan
  implementasi deployment (TD-007).
- Prioritaskan perbaikan security (TD-001, TD-002) karena berpotensi menyebabkan
  kerugian finansial/data breach.
- Selesaikan masalah migration runner (TD-035 s/d TD-037): migration 010 & 011
  (dan file semacamnya) harus di-apply via psql (auto-commit) + force version;
  bangun custom runner/wrapper scripts/migrate_with_psql.sh agar deploy tidak
  lagi manual & otomatis terdeteksi (CONCURRENTLY / ALTER TYPE ADD VALUE).

3. RINGKASAN STATUS TERKINI (2026-09-20):
   - Total hutang teridentifikasi      : 41 item.
   - Sudah FIXED/CLOSED                : 22 item (TD-001 s/d TD-006, TD-008, TD-009, TD-011 s/d TD-021, TD-023, TD-041, TD-044).
   - DEFERRED                          : 1 item (TD-010 - Flutter coverage instability).
   - ❌ OPEN                           : 18 item (TD-007 - deployment backend endpoint belum
                                          dilakukan; TD-022 - Language toggle UI; TD-024,
                                          TD-025, TD-026, TD-027 - admin auth flow, STEP 1C-1F;
                                          TD-030 - ganti password default admin; TD-031 -
                                          bcrypt hash hardcoded di migration 014; TD-032 -
                                          docker-compose paksa platform linux/amd64; TD-033 -
                                          POSTGRES_PASSWORD hardcoded; TD-034 - redis belum
                                          upgrade ke 7.4; TD-035 - ALTER TYPE ADD VALUE di
                                          migration 010; TD-036 - CREATE INDEX CONCURRENTLY di
                                          migration 011; TD-037 - golang-migrate tanpa --no-tx;
                                          TD-042 - AuthGuard redirect ke /; TD-043 - DESIGN
                                          ADMIN WEB.md:995 stale; TD-045 - API CONTRACT §10
                                          outdated; TD-046 - dashboard error_rate stub 0.0).

   CATATAN:
   - STEP 1B menambahkan TD-023 (FIXED - 2FA secret via ADMIN_2FA_SECRET) dan
     TD-024 s/d TD-027 (OPEN - rencana STEP 1C-1F: admin seed, login+2FA endpoint,
     lockout refactor, admin web login).
   - STEP 1C (2026-09-20) menambahkan TD-030 & TD-031. Migration seed admin
     (014_seed_admin.up.sql) dibuat, belum dijalankan (menunggu user).
   - STEP 1D (2026-09-20) menambahkan TD-032 s/d TD-034 (infra/security
     docker-compose.yml: platform linux/amd64, POSTGRES_PASSWORD hardcoded,
redis belum 7.4) seiring align versi Postgres docker-compose → 15.
   - MIGRATION (2026-09-20) menambahkan TD-035 s/d TD-037 (INFRA/migration):
      apply migration 010 (ALTER TYPE ADD VALUE) & 011 (CREATE INDEX CONCURRENTLY)
      gagal di transaction block golang-migrate → harus manual via psql
      (auto-commit) + force version. Sumber masalah: migrate CLI tanpa dukungan
      --no-tx per file.
   - TEAM CONTRACT (2026-09-20) menambahkan TD-041 (DOKUMENTASI, FIXED):
      rewrite docs/TEAM_CONTRACT.md v2.0 — Developer RULES (Terminal Safety,
      Hallucination Detection, Konteks & Scope, Escalation, Learning Checkpoint,
      Autonomy Boundaries) + 10 RULES + peran/alur/formula. Contract jadi
      SELF-CONTAINED untuk OpenCode.
- ADMIN WEB 2FA (2026-09-20) menambahkan TD-042 (KODE, OPEN): AuthGuard belum
       redirect balik ke / kalau user sudah login buka /login (UX improvement),
       dan TD-043 (DOKUMENTASI, OPEN): docs/UI UX/DESIGN ADMIN WEB.md:995 stale
       (menyebut /admin/login + deskripsi klinik, kemungkinan DentFlow).
    - STEP A1 v2 (2026-09-21) menambahkan TD-046 (KODE, OPEN): dashboard KPI
      error_rate masih stub 0.0 karena error tracking belum ada.
    - TD-045 (2026-09-21) menambahkan TD-045 (DOKUMENTASI, OPEN): API
      CONTRACT.txt §10 (Admin Endpoints) outdated vs implementasi ROADMAP 04
      (rute /api/v1/admin/*).
    - STATUS: SIAP LIVE DEMO / PORTOFOLIO (celah admin auth masih OPEN, TD-024 s/d TD-027).

================================================================================
END OF TECHNICAL DEBT REGISTER v2.0 (Phase 1-3)
================================================================================
