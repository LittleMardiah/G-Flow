run:
  timeout: 5m
  tests: true
  go: '1.26'
linters:
  disable-all: true
  enable:
    - govet
    - ineffassign
    - unused
issues:
  exclude-rules:
    - path: _test\.go
      linters:
        - unused